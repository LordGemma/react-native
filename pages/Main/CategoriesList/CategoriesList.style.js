import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  categories: {
    flexDirection: 'row',
    padding: 20,
    borderBottomColor: '#ebebeb',
    borderBottomWidth: 8,
  },
});
