import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginRight: 20,
  },
  title: {
    textTransform: 'uppercase',
    fontSize: 12,
    marginTop: 10,
  },
});
